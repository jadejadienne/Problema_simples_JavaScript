const num = [1, 2, 3, 4, 5];


function somaArray(arr) {
    let soma = 0;
    for (let i = 0; i < arr.length; i++) {
        soma += arr[i];
    }
    return soma;
}

const resultado = somaArray(num);
console.log("A soma dos elementos do array é:", resultado);
